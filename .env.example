DEEPSEEK_API_KEY=your-deepseek-api-key
TAVILY_API_KEY=your-tavily-api-key

# Generate a Tavily API key for live web search:
# https://app.tavily.com/home

# Optional future integration with the separate SmartStore Product Data API.
# These values are used on the server only and must never be exposed to the browser.
SMARTSTORE_PRODUCTS_API_URL=
SMARTSTORE_PRODUCTS_API_KEY=
